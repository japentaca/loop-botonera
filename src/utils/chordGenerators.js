/**
 * Chord Generators (stub PoC)
 */
export function assignChordTones(chord, length = 16) {
  // Minimal placeholder: return an array of nulls for now
  return new Array(length).fill(null)
}

export default { assignChordTones }
